---
alwaysApply: false
description: Project structure and file organization conventions
---
# Project Structure & Organization

## Directory Structure

From the root directory:
- Use the `/app` directory for all routes and layouts (Next.js 13+ app router).
- Place shared components in `/components`.
- Place hooks in `/hooks`.
- Place utility functions in `/lib`.
- Use `/public` for static assets.

## File Naming

- Use descriptive names for files, variables, and functions.
- Component files should use PascalCase (e.g., `UserProfile.tsx`).
- Utility files should use kebab-case (e.g., `db-utils.ts`).
- Hook files should use kebab-case with `use-` prefix (e.g., `use-mobile.ts`).

## Import Organization

- Group imports: external libraries, then internal modules, then styles.
- Sort imports alphabetically within groups.
- Use ES modules (`import`/`export`).
